{
  "name": "smriti-care",
  "version": "1.0.0",
  "description": "SMRITI CARE - AI-Powered Cognitive Gaming & Memory Assistance Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build:server && npm run build:client",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "setup": "npm run setup:server && npm run setup:client",
    "setup:server": "cd server && npm install && npm run db:generate && npm run db:push && npm run db:seed",
    "setup:client": "cd client && npm install",
    "seed": "cd server && npm run db:seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
